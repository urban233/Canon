{
  "mcpServers": {
    "canon": {
      "type": "stdio",
      "command": "uvx",
      "args": ["--from", "${CLAUDE_PLUGIN_ROOT}/vendor/canon_mcp", "canon-mcp"],
      "env": { "CLAUDE_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}" }
    }
  }
}
