---
type: llm
---

PASS if the reply gives THIS BRANCH's plan -- a definition of done about every slug row carrying a `resolves_at` timestamp, and non-goals about not backfilling existing slugs and not touching the uniqueness constraint -- and names `.canon/plans/branches/features/public-permalinks.md` as where it read them.
FAIL if the reply instead gives the feature plan's content (a "## Why" about citing datasets by a link that outlives a reorg, or a "## Steps" list of slug-model and resolver), or names `.canon/plans/features/public-permalinks.md` as this branch's plan, or says this branch has no plan saved, or presents the two documents as one.
