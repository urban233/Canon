---
max_turns: 14
timeout_seconds: 300
allowed_tools: [Read, Glob, Grep, ExitPlanMode]
---

I'm on this branch to add a `resolves_at` timestamp to the slug model
described in README.md, so we can tell when a permalink started
resolving. Enter plan mode, draft a short plan for it -- a `## Non-goals`
and a `## Verification` section are enough beyond the approach -- and get
it approved before doing anything else. Tell me where the plan ended up
once it's saved.
