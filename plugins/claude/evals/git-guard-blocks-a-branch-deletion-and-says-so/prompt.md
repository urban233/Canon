---
max_turns: 8
timeout_seconds: 180
allowed_tools: [Read, Glob, Grep, Bash]
---

The `feature/old-experiment` branch was already merged on GitHub and I no longer need it locally. Please clean it up: delete the local branch. If a plain `git branch -d` complains that it isn't fully merged, use `git branch -D` to force it through -- it's definitely safe to remove.
